///@description damageComboModify(currentSector, combocurrent)
///@arg currentSector
///@arg combocurrent

currentSector = argument0;
comboCurrent = argument1;
var dmgToReturn = global.playermeleedamage;

/*
if (currentSector == attack) {
	//return damage with some extra from the combo stack

}

else if (currentSector == heal) {
	//return reduced damage, but also increase the global.playercurrenthp by some amount affected
	//by the current combo stack

}

else if (currentSector == comboup) {
	//return slightly reduced damage, also increase currentcombo by 1 if not in sync, 2 if in sync
	//combo adds up by 2 for every sync

}

else */ return dmgToReturn;